version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - REDIS_URL=redis://redis:6379
      - USE_REDIS=true
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
